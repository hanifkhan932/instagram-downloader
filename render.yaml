services:
  - type: web
    name: instagram-downloader
    runtime: python
    buildCommand: pip install yt-dlp && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18